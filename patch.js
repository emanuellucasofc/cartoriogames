const fs = require('fs'); let code = fs.readFileSync('js/render.js', 'utf8'); code = code.replace('<header class="head">', '<header class="head" id="main-header">'); code = code.replace('data-panel="fila">\'', 'data-panel="fila" id="panel-fila">\''); const tick = \nCS.renderTick = function() {\n  var header = document.getElementById('main-header');\n  if (header) {\n    var dummy = document.createElement('div');\n    dummy.innerHTML = CS.renderHeader();\n    header.parentNode.replaceChild(dummy.firstChild, header);\n  }\n  var fila = document.getElementById('panel-fila');\n  if (fila) {\n    fila.innerHTML = CS.renderQueueAndLog();\n  }\n  if (CS.updateOfficeSprites) CS.updateOfficeSprites();\n};\n; if (!code.includes('CS.renderTick')) { code += tick; } fs.writeFileSync('js/render.js', code);
